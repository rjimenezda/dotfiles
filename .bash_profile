# Some aliases 
alias ll="ls -lah"
alias emacs="echo \"In vim no one can hear you C-x C-c\" |vim -"

# Pretty colors
export CLICOLOR=1

# NVM config
export NVM_DIR="/Users/romanjimenez/.nvm"
[ -s "$NVM_DIR/nvm.sh" ] && . "$NVM_DIR/nvm.sh"  # This loads nvm

cowsay "undefined is not a function"
